configurations.maybeCreate('default')

fileTree(dir: '.', include: '*.aar').each { aar ->
    artifacts.add('default', aar)
}

tasks.register('clean', Delete) {
    delete project.layout.buildDirectory
}
