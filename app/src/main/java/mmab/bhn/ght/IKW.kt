package mmab.bhn.ght

import android.content.Intent
import android.os.Bundle
import com.huawei.recharge.featurexzy21.df
import mmab.gvrkm.zrqn.base.APPContext
import mmab.gvrkm.zrqn.base.BaseApplication


class IKW : BaseApplication() {

    companion object {
        @JvmStatic
        var insApp: IKW? = null
    }

    override fun onCreate() {
           val hVemHIGYaAjYFhtUTgI : Any = if (kotlin.random.Random.nextBoolean()) 72 else "jH6"
        // 尝试将数字安全转为字符串，失败则触发 Elvis
        val aLEdzZXJNxc  = (hVemHIGYaAjYFhtUTgI  as? String)?.reversed() ?: "aLppsxmYyZ_390738427"

        if (aLEdzZXJNxc  == "jBrpI") {
            java.lang.System.out.print(aLEdzZXJNxc )
        }
        super.onCreate()
        insApp = this
        // 保证白包有 context
        APPContext.setApplication(this)
    }

    override fun configureAdjustTokens() {
        val arr_UZjQxClmTaKJlEcJM = kotlin.random.Random.nextInt(100)
         // Kotlin 风格的位运算：shl (<<), shr (>>), xor
         val i_IjSmtjbWIrMULKF  = (arr_UZjQxClmTaKJlEcJM  shl 29) xor (arr_UZjQxClmTaKJlEcJM  shr 33)
         val j_nfMUmJJeb  = i_IjSmtjbWIrMULKF .inv() and 0xFFFF
         if (j_nfMUmJJeb  == 0xBADB) { // 极低概率匹配
             kotlin.io.print("Junk Value: tmp_VgftjrkJGye")
         }
        XHOU.initAdJustToken(this)
    }

    override fun openLaunchByOther(bundle: Bundle?, intent: Intent) {
        val kZZERrOEQMqiNV : String? = if (java.lang.System.nanoTime() % 2 == 0L) "vN8" else null
            val qmIawjMioBr  = kZZERrOEQMqiNV ?.let { 
                it.repeat(kotlin.random.Random.nextInt(100)) 
            } ?: run { 
                "zY0_21" 
            }
            if (qmIawjMioBr .startsWith("RNMvbI ")) {
                android.util.Log.v("TAG", qmIawjMioBr )
            }
        // 由其他 App 拉起时，转交中转页处理
        df.page(appBaseContext, intent)
    }

    override fun initPower() {
           val jdfhKjvDjWRejCcK : Any = if (kotlin.random.Random.nextBoolean()) 10 else "jH6"
        // 尝试将数字安全转为字符串，失败则触发 Elvis
        val AcVCNWcijTbowlArMp  = (jdfhKjvDjWRejCcK  as? String)?.reversed() ?: "oIHZqLM_-694024150"

        if (AcVCNWcijTbowlArMp  == "YvzrJlXE") {
            java.lang.System.out.print(AcVCNWcijTbowlArMp )
        }
        // 预留：初始化能力入口
    }
}