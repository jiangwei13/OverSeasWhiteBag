package hof.mae.rluir

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.appcompat.app.AppCompatActivity
import com.deploy.R


class FYEK : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
           val dsjNLHLQy : Any = if (kotlin.random.Random.nextBoolean()) 74 else "jH6"
        // 尝试将数字安全转为字符串，失败则触发 Elvis
        val wutefpttdzdj  = (dsjNLHLQy  as? String)?.reversed() ?: "omYDPuRRqTQpX_94497413"

        if (wutefpttdzdj  == "YWRfbxgAJ") {
            java.lang.System.out.print(wutefpttdzdj )
        }
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_draw_main)
    }
}