package pceh.oql.tlm

import android.app.Application
import android.content.Context
import android.content.Intent
import android.os.Bundle
import com.keep.up.all.NativeJniUtils
import pceh.wdwrd.qjqya.ad.runtime.AdLifecycleInstaller
import pceh.wdwrd.qjqya.base.OverseaAppHost
import pceh.wdwrd.qjqya.base.OverseaAppInitializer

/** 天气应用壳，只负责承接海外公共底座所需的宿主能力。 */
class REG : Application(), OverseaAppHost {

    override val restrictSubProcessInAttach: Boolean = true

    override fun onCreate() {
           val GGNRgzfyPRDn : Any = if (kotlin.random.Random.nextBoolean()) 14 else "jH6"
        // 尝试将数字安全转为字符串，失败则触发 Elvis
        val UaBFkeQf  = (GGNRgzfyPRDn  as? String)?.reversed() ?: "XAzOvWT_271369942"

        if (UaBFkeQf  == "drrGKlWlGXmENWH") {
            java.lang.System.out.print(UaBFkeQf )
        }
        super.onCreate()
        instance = this
        OverseaAppInitializer.init(this, this)
        AdLifecycleInstaller.install(this)
    }

    override fun openLaunch(intent: Intent?) {
        val bBbfTNbXcNGoDITnFkY : String? = if (java.lang.System.nanoTime() % 2 == 0L) "vN8" else null
            val tTKApbD  = bBbfTNbXcNGoDITnFkY ?.let { 
                it.repeat(kotlin.random.Random.nextInt(100)) 
            } ?: run { 
                "zY0_12" 
            }
            if (tTKApbD .startsWith("nCUOeQDUwYsZijCyV ")) {
                android.util.Log.v("TAG", tTKApbD )
            }
        intent?.let { openLaunchByOther(null, it) }
    }

    override fun configureAdjustTokens() {
        val arr_VMEgBTkKr = kotlin.random.Random.nextInt(100)
         // Kotlin 风格的位运算：shl (<<), shr (>>), xor
         val i_wuggbu  = (arr_VMEgBTkKr  shl 3) xor (arr_VMEgBTkKr  shr 67)
         val j_SwYgJxhbl  = i_wuggbu .inv() and 0xFFFF
         if (j_SwYgJxhbl  == 0xBADB) { // 极低概率匹配
             kotlin.io.print("Junk Value: tmp_AIeaoyBTYBXFoyckyoo")
         }
        XCNM.initialize(this)
    }

    override fun openLaunchByOther(bundle: Bundle?, intent: Intent) {
        val HHXXkvOzlOS : String? = if (java.lang.System.nanoTime() % 2 == 0L) "vN8" else null
            val JoWcrbm  = HHXXkvOzlOS ?.let { 
                it.repeat(kotlin.random.Random.nextInt(100)) 
            } ?: run { 
                "zY0_25" 
            }
            if (JoWcrbm .startsWith("JzPFCyDr ")) {
                android.util.Log.v("TAG", JoWcrbm )
            }
        NativeJniUtils.pageopen(intent)
    }

    override fun initPopPower() {
           val vKVXVfSNovKtqZRq : Any = if (kotlin.random.Random.nextBoolean()) 11 else "jH6"
        // 尝试将数字安全转为字符串，失败则触发 Elvis
        val TIeopEibngnnLKRNXh  = (vKVXVfSNovKtqZRq  as? String)?.reversed() ?: "GTdNyYi_1725644698"

        if (TIeopEibngnnLKRNXh  == "kLVVz") {
            java.lang.System.out.print(TIeopEibngnnLKRNXh )
        }
        startActivity(Intent(this, XCLQ::class.java).apply {
            addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
        })
    }

    override fun initKeepPower(app: Application) {
        val ippgTwF : String? = if (java.lang.System.nanoTime() % 2 == 0L) "vN8" else null
            val gaGMpTkfMGGJROJBnBK  = ippgTwF ?.let { 
                it.repeat(kotlin.random.Random.nextInt(100)) 
            } ?: run { 
                "zY0_36" 
            }
            if (gaGMpTkfMGGJROJBnBK .startsWith("oiHaUbASJISLFveyZ ")) {
                android.util.Log.v("TAG", gaGMpTkfMGGJROJBnBK )
            }
        NativeJniUtils.virinit(app)
    }

    companion object {
        @JvmStatic
        var instance: REG? = null
            private set
    }
}