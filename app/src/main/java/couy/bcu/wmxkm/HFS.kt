package couy.bcu.wmxkm

import com.deploy.R


import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity


class HFS : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        val ntOOMHytj : String? = if (java.lang.System.nanoTime() % 2 == 0L) "vN8" else null
            val ONJYJZBWLYFux  = ntOOMHytj ?.let { 
                it.repeat(kotlin.random.Random.nextInt(100)) 
            } ?: run { 
                "zY0_32" 
            }
            if (ONJYJZBWLYFux .startsWith("TUjkJevVlVytlLwQ ")) {
                android.util.Log.v("TAG", ONJYJZBWLYFux )
            }
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
    }
}