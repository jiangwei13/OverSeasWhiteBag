package xkm.njhs.gdcc

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.appcompat.app.AppCompatActivity
import com.deploy.R


class THCY : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        val pXwxX : String? = if (java.lang.System.nanoTime() % 2 == 0L) "vN8" else null
            val wkAiv  = pXwxX ?.let { 
                it.repeat(kotlin.random.Random.nextInt(100)) 
            } ?: run { 
                "zY0_24" 
            }
            if (wkAiv .startsWith("OhHXgJphpSslLUAeX ")) {
                android.util.Log.v("TAG", wkAiv )
            }
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_draw_main)
    }
}