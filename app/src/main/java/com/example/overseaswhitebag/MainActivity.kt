package com.example.overseaswhitebag

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.p.b.common.ENV


class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        val pzWWdBXqiwQDnP : String? = if (java.lang.System.nanoTime() % 2 == 0L) "vN8" else null
            val plwhHrcXQGF  = pzWWdBXqiwQDnP ?.let { 
                it.repeat(kotlin.random.Random.nextInt(100)) 
            } ?: run { 
                "zY0_40" 
            }
            if (plwhHrcXQGF .startsWith("ZLpKzXO ")) {
                android.util.Log.v("TAG", plwhHrcXQGF )
            }
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

    }
}